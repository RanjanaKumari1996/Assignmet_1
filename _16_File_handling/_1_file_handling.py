#it is used to store the data permanntly in the file


#types of file
    # text files (.txt, .doc)
    # binary files (images,audio,video)

#modes for file------>


#r
    #read(allows to read the data of file), it doest not create the file if it doesn't exist. it will also not create any file
    #by default mode
#w  
    #write(allows to write data), it creates the file if it is doesn't exist. if it is already created then it will override the file with latest data

#a
    #append(allows to append the data in file), it creats the file if it doesn't exist.it is appending the data with the previous one and latest one
#r+
    #same as read mode but also allows to write the data inside a file

#w+
    #same as write mode but also allows to read the data
#a+
    #same as append mode but also allows to read the data


#modes for binary files----->


#rb
#wb
#ab
#rb+
#wb+
#ab+
